{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_FILE_PATH\" | grep -q \"supabase-schema.sql\"; then echo \"BLOCK: supabase-schema.sql is protected. Use Supabase migrations instead.\"; exit 2; fi'"
          }
        ]
      }
    ]
  }
}
